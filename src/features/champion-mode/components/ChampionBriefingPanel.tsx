import React from 'react';
import { CompetitionBriefPanel } from './CompetitionBriefPanel';

export const ChampionBriefingPanel: React.FC = (props) => {
    return <CompetitionBriefPanel {...props} />;
};

export default ChampionBriefingPanel;
